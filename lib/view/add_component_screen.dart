import 'package:flutter/material.dart';

class AddComponentScreen extends StatelessWidget {
  const AddComponentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Thêm linh kiện mới"),
      ),
    );
  }
}